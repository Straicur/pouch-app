framework:
    rate_limiter:
        login:
            policy: 'sliding_window'
            limit: 5
            interval: '15 minutes'
        refresh_token:
            policy: 'sliding_window'
            limit: 10
            interval: '15 minutes'

when@test:
    framework:
        rate_limiter:
            login:
                policy: 'sliding_window'
                limit: 1000
                interval: '15 minutes'
            refresh_token:
                policy: 'sliding_window'
                limit: 1000
                interval: '15 minutes'
